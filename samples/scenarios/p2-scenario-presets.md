# P2 Scenario Presets Sample

These presets are available from the State Sequencer scenario dropdown.

```text
Protection fault
Prefault 0.200 s, I=1 A, V=57.735 V LN, f=50 Hz
Fault    0.120 s, I=5 A, V=11.547 V LN, f=50 Hz
Recovery 0.250 s, I=1 A, V=57.735 V LN, f=50 Hz
```

```text
Frequency steps
49.0 Hz -> 49.5 Hz -> 50.0 Hz -> 50.5 Hz -> 51.0 Hz
```

The presets are operator-editable after applying them.
